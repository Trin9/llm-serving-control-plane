apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "monitoring-stack.fullname" . }}-grafana-dashboard
  labels:
    {{- include "monitoring-stack.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  llm-serving-monitor.json: |
{{ .Files.Get "dashboards/llm-serving-monitor.json" | indent 4 }}