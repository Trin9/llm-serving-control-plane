✅ LLM Operator installed successfully!

📦 Release Name: {{ .Release.Name }}
📂 Namespace: {{ .Release.Namespace }}

🎯 Next steps:

1. Check the Operator status:
   kubectl get deployment -n {{ .Release.Namespace }} {{ include "llm-operator.fullname" . }}-controller-manager

2. View the Operator logs:
   kubectl logs -n {{ .Release.Namespace }} -l control-plane=controller-manager -f

3. Verify the CRD was created:
   kubectl get crd inferenceservices.serving.trin.io

4. Create a sample InferenceService:
   kubectl apply -f - <<EOF
   apiVersion: serving.trin.io/v1
   kind: InferenceService
   metadata:
     name: test-model
     namespace: {{ .Release.Namespace }}
   spec:
     modelName: "test-model"
     engine: "mock"
     replicas: 1
   EOF

5. Check the InferenceService status:
   kubectl get inferenceservices -n {{ .Release.Namespace }}
   kubectl describe inferenceservice test-model -n {{ .Release.Namespace }}

📚 Docs: https://github.com/trin/llm-serving-control-plane

🐛 Issues: check the Operator logs or file an Issue
