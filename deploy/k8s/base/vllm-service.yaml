apiVersion: v1
kind: Service
metadata:
  name: vllm-qwen-service # fixed DNS name used by the Gate Service
spec:
  selector:
    app: vllm-qwen # local and AWS Pods must all carry this label
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP