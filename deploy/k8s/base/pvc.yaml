apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "" # leave empty; determined by the specific PV
  resources:
    requests:
      storage: 100Gi